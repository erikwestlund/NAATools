% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/submission_characterization.R
\name{calculate_frequencies}
\alias{calculate_frequencies}
\title{Calculate unique value frequencies per column (Fast Version with data.table)}
\usage{
calculate_frequencies(data, freq_cols, required_cols, col_types)
}
\arguments{
\item{data}{A data frame.}

\item{freq_cols}{A character vector specifying which columns to analyze.}

\item{required_cols}{A character vector of required columns that should exist in the data (missing ones are created as NA).}

\item{col_types}{A named character vector specifying expected column types (e.g., \code{c("col1" = "numeric", "col2" = "string")}).}
}
\value{
A data table where each unique value in \code{freq_cols} has:
\itemize{
\item All \code{freq_cols} with their unique values.
\item \verb{_n} (count of occurrences in the data).
\item \verb{_pct} (percentage of occurrences in the data).
}
}
\description{
Computes frequency counts and percentages for each unique value in the specified columns,
ensuring that the columns match the specified types.
}
\examples{
df <- data.frame(
  category = c("A", "A", "B", "B", "C"),
  type = c("X", "Y", "X", "Y", "X"),
  group = c(1, 2, 1, 2, 3)
)
col_types <- c("category" = "string", "type" = "string", "group" = "numeric")
result <- calculate_frequencies(df, c("category", "type", "group"), NULL, col_types)
print(result)
}
