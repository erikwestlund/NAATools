% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_test_data.R
\name{generate_test_data}
\alias{generate_test_data}
\title{Generate a synthetic dataset based on a frequency table}
\usage{
generate_test_data(
  freq_table,
  n = NA,
  extra_cols = list(),
  count_col = NULL,
  col_order = NULL
)
}
\arguments{
\item{freq_table}{A data frame or data.table containing the frequency table.}

\item{n}{The number of rows to generate (if NA, generates 1 row per unique frequency combo).}

\item{extra_cols}{A named list where each key is a column name to add and
the value is a vector of predefined values that will be recycled or sampled.}
}
\value{
A data.table with `n` rows based on the frequency table.
}
\description{
This function generates a synthetic dataset based on a frequency table.
If `n = NA`, it returns one row per unique frequency combination.
If `n >= total`, it generates data proportionally based on row counts.
If `n < total`, it samples while trying to maintain proportions.
}
\examples{
library(data.table)
freq_table <- data.table::data.table(
  category = c("A", "B", "C"),
  type = c("X", "Y", "X"),
  group = c(1, 2, 1)
)

test_data <- generate_test_data(freq_table, n = 100)
print(test_data)
}
